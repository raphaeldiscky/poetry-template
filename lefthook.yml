pre-commit:
  commands:
    format:
      run: task format
    lint:
      run: task lint
    stage:
      run: git add -A .

pre-push:
  commands:
    test:
      run: task test

commit-msg:
  commands:
    commitlint:
      run: npx --no-install commitlint --edit {1}
